for vim
$ find . -name solarized.vim
$ mkdir -p ~/.vim/colors; cp <solarized.vim> ~/.vim/colors
$ vim ~/.vimrc
add
====
syntax enable
if has('gui_running')
    set background=light
else
    set background=dark
endif
set t_Co=256
let g:solarized_termcolors=256
colorscheme solarized
====
